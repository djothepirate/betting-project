# CI-002 — Fiabiliser et verrouiller l'analyse des dépendances

- **Statut :** Qualification locale terminée — Pull Request à ouvrir
- **Date d'ouverture :** 2026-09-02
- **Responsable de décision :** Porteur du Betting Project
- **Exécutant :** Codex
- **ADR applicables :** ADR-005, ADR-007, ADR-008
- **Branche :** `codex/ci-002-nvd-dependency-check`

## Objectif

Rendre l'analyse OWASP Dependency-Check reproductible sur GitLab sans inscrire ni afficher de clé
NVD, résorber la baseline élevée et critique, puis verrouiller le ratchet qui refuse toute
vulnérabilité de score CVSS supérieur ou égal à 7.

## Contexte et décision d'alimentation

Le pipeline GitLab `#2813456672`, exécuté sur `main` au SHA `c8ccf050`, a échoué dans
`security:dependencies`. Dependency-Check `13.0.0` transforme l'absence de clé en chaîne vide et
échoue avant de pouvoir initialiser sa base locale. Cette régression amont est corrigée sur la
branche principale du projet Dependency-Check, mais sa livraison `13.0.1` n'est pas encore publiée.

Le projet GitLab ne contient aucune variable CI/CD. Un premier essai réel sans clé, sur le SHA
`390fd1c5ba5e271a839cb4cfd9cd30a51033b343`, a confirmé que `12.2.2` ne transmet aucune chaîne vide,
mais l'API anonyme a répondu HTTP 429 après 240 000 CVE sur 385 681 et environ 37 minutes. Un cache
qui dépend de cette initialisation n'est donc pas une alimentation exploitable.

Le lot retient l'autre alternative sans secret :

- revenir temporairement à Dependency-Check `12.2.2`, dernière version publiée avant la
  régression ;
- alimenter l'outil directement depuis les flux JSON 2.0 publics officiels du NVD avec le motif
  `https://nvd.nist.gov/feeds/json/cve/2.0/nvdcve-2.0-{0}.json.gz` ;
- conserver la base NVD sous `.m2/dependency-check-data/` dans un cache GitLab propre à la ref et à
  la version de l'outil et du format de flux ;
- ne transmettre aucune option de clé, que `NVD_API_KEY` soit absente, vide ou présente ;
- mettre à jour Spring Boot 4.1.0 vers 4.1.1, qui corrige Spring Framework, pgJDBC et Log4j, puis
  surcharger Tomcat à 11.0.25 jusqu'à son intégration dans le BOM ;
- réexaminer `13.0.1` dans un lot distinct après sa publication.

Une future migration vers l'API exigerait un lot distinct et une variable de projet GitLab masquée,
cachée si disponible, protégée, non développée et limitée à ce seul projet. La branche `main`
restant volontairement non protégée selon ADR-008, cette variable n'y serait pas accessible ; le
flux public est donc l'alimentation opérationnelle actuelle et le cache n'est qu'une optimisation.

## Périmètre autorisé

- ajouter un lanceur Dependency-Check qui ne transporte jamais la valeur de la clé NVD ;
- couvrir les cas clé absente, vide et présente par des tests de contrat ;
- utiliser le flux JSON 2.0 officiel comme source reconstructible ;
- isoler et conserver la base NVD dans un cache GitLab par ref ;
- mesurer la baseline élevée/critique sur le SHA du lot ;
- mettre à jour les dépendances affectées sans sortir du BOM, sauf surcharge Tomcat documentée ;
- versionner uniquement les exceptions exactes éventuellement nécessaires, avec justification et
  échéance ;
- retirer `allow_failure` et fixer le seuil bloquant à CVSS 7 après mesure de la baseline ;
- documenter l'exploitation, la rotation et la limite liée aux refs protégées.

## Exclusions

- aucune clé, valeur de variable, empreinte de clé ou fixture ressemblant à un secret dans Git ;
- aucune lecture du fichier local de clés API du benchmark ;
- aucune création automatique d'une clé auprès de NVD ;
- aucun cache Maven général partagé entre branches ;
- aucune modification des protections `main` et `release/*` décidées par ADR-008 ;
- aucune suppression globale ou non bornée d'une vulnérabilité ;
- aucune fusion automatique de la Pull Request.

## Critères d'acceptation

- [x] La cause exacte du pipeline `#2813456672` est enregistrée.
- [x] Dependency-Check n'utilise plus la version `13.0.0` défectueuse.
- [x] Une clé NVD absente ou vide ne produit aucun argument de clé.
- [x] Une clé présente dans l'environnement est ignorée par la voie flux et sa valeur n'entre jamais
  dans la ligne de commande.
- [x] L'échec HTTP 429 prouve que l'API anonyme avec cache seul n'est pas retenue comme alimentation.
- [x] Le flux JSON 2.0 officiel initialise la base et produit les rapports HTML, JSON et GitLab.
- [x] La base NVD est conservée dans un cache GitLab versionné et propre à la ref.
- [x] ADR-008 borne explicitement ce cache à la base publique NVD et maintient l'interdiction du
  cache du dépôt Maven général.
- [x] La baseline élevée/critique est mesurée et enregistrée sur un SHA précis.
- [x] Les dépendances affectées sont corrigées sans exception ni règle de suppression.
- [x] Le seuil CVSS 7 est bloquant et `allow_failure` est retiré après la mesure.
- [x] Les tests standards, d'intégration, de secrets et de contrat CI sont verts.
- [ ] La Pull Request dédiée reste soumise à revue humaine.

## Commandes de test

```text
sh ci/test-dependency-check.sh
sh ci/test-gitlab-workflow.sh
./scripts/check-no-secrets.sh --scope all --base origin/main
./mvnw verify
./mvnw -Pintegration verify
sh ci/run-dependency-check.sh
git diff --check origin/main...HEAD
```

## Séquence du ratchet

1. Introduire le lanceur, le cache isolé et les tests en conservant le seuil d'observation 11 et
   `allow_failure: true`.
2. Charger la base depuis le flux JSON 2.0 et produire un rapport complet sur le SHA d'observation.
3. Recenser les seules vulnérabilités CVSS 7 à 10. Les corriger si possible ; sinon créer une
   exception exacte et temporaire, soumise à la décision humaine.
4. Dans un commit distinct, fixer `failBuildOnCVSS=7`, refuser les exceptions devenues inutiles et
   retirer `allow_failure`.
5. Qualifier le diff complet ; la baisse du seuil ou l'élargissement d'une exception exige une
   nouvelle décision humaine motivée.

## Résultats d'exécution

### Baseline d'observation

- SHA : `390fd1c5ba5e271a839cb4cfd9cd30a51033b343` ;
- fin du scan : 2026-09-02 17:11:54 +02:00 ;
- source : flux JSON 2.0 public officiel NVD ;
- durée du premier chargement et de l'analyse : 7 min 09 s ;
- rapport JSON SHA-256 :
  `d0ca9c217dc4b98029d509ab72c82d1458eab0b1e0d42a95dada06d01c437d9c` ;
- résultat : 27 CVE sur 4 dépendances, dont 11 critiques, 8 élevées, 7 moyennes et 1 faible ;
- dette bloquante initiale : 19 CVE de score supérieur ou égal à 7, maximum 9,8.

| Dépendance observée | CVE | Correction retenue |
| --- | ---: | --- |
| `log4j-api-2.25.4.jar` | 1 | Spring Boot 4.1.1 → Log4j 2.25.5 |
| `postgresql-42.7.11.jar` | 1 | Spring Boot 4.1.1 → pgJDBC 42.7.13 |
| `spring-core-7.0.8.jar` | 6 | Spring Boot 4.1.1 → Spring Framework 7.0.9 |
| `tomcat-embed-core-11.0.22.jar` | 19 | surcharge BOM → Tomcat 11.0.25 |

Aucune suppression n'est créée. Les résultats de qualification après correction sont complétés
avant l'ouverture de la Pull Request.

### Qualification après correction

- arbre Maven résolu : Spring Framework 7.0.9, Tomcat 11.0.25, pgJDBC 42.7.13 et Log4j 2.25.5 ;
- scan avec `failBuildOnCVSS=7` : succès en 17,421 s sur l'index chaud, zéro dépendance vulnérable ;
- rapport JSON corrigé SHA-256 :
  `1fcf49d55c0f7ff339f14723ed6a39ea84d95d1dbbf2ca08f5e9704129d5802a` ;
- `scripts/verify-windows.cmd` : succès avec Maven 3.9.16 et Java 25.0.4 ;
- tests standards : 219 réussis, 0 échec, 0 erreur, 0 ignoré ;
- tests PostgreSQL/Testcontainers : 89 réussis, 0 échec, 0 erreur, 0 ignoré ;
- cinq migrations Flyway validées sur PostgreSQL 17.10 ;
- contrôle de secrets : succès ;
- contrats `ci/test-dependency-check.sh` et `ci/test-package-guards.sh` : succès ;
- syntaxe shell et `git diff --check origin/main` : succès.

Les rapports générés restent des artefacts de qualification sous `target/` et ne sont pas
versionnés. Le SHA du commit de ratchet et le lien de Pull Request sont ajoutés après leur création.

## Définition de terminé

La Pull Request est prête pour revue lorsque la base NVD est alimentée, que le rapport de baseline
est rattaché à son SHA, que les vulnérabilités élevées ou critiques nouvelles font échouer le job et
que les tests prouvent l'absence de clé vide ou en clair dans les arguments. La fusion relève de la
revue humaine prévue par ADR-005.
